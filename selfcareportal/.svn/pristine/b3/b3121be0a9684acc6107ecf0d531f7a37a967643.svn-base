// Defining the application router.
define(function(require) {
    // External dependencies.
    var AppView = require('AppView');

    // Defining the application router.
    var Router = fish.Router.extend({
        routes: {
            "": "index"
        },

        index: function() {
            new AppView().render();
        }
    });

    return Router;
});
